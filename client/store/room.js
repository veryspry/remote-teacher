import axios from 'axios'
import history from '../history'

/**
 * ACTION TYPES
 */

/**
 * INITIAL STATE
 */
const initialState = {
  roomName: '',
}

/**
 * ACTION CREATORS
 */

/**
 * THUNK CREATORS
 */


/**
 * REDUCER
 */
export default function(state = defaultUser, action) {
  switch (action.type) {
      
    default:
      return state
  }
}
